@page "/tools/binder"

<PageTitle>Binder</PageTitle>
<h2 class="page-title">Binder</h2>
<p class="page-subtitle">
    The types <code>IBinder</code> and <code>Bindable&lt;TComponent,TValue&gt;</code> allow you to bind multiple properties to your components at once.
    The binding logic is segregated so your component logic can stay nice and clean.
</p>

<div class="page-examples">
    <ExampleViewer Title="Example" Resource="Tools.Binder" />
</div>

<div class="page-members">
    <MembersViewer Type="typeof(IBinder)" />
    <MembersViewer Type="typeof(IBinderConfiguration)" />
    <MembersViewer Type="typeof(Binding<,>)" />
    <MembersViewer Type="typeof(Bindable<>)" />
</div>
