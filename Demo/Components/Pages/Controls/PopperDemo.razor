@page "/controls/popper"

<PageTitle>Popper</PageTitle>
<h2>Popper</h2>
<p>Create a pop-out view with the <code>Popper</code> component.</p>

<h4>Example</h4>
<p>Use the <code>Trigger</code> fragment to define a trigger. When clicked the <code>Content</code> fragment will be shown.</p>
<div class="popper-example">
    <Popper @bind-Expanded="Expanded" CollapseOnFocusLost>
        <Trigger>Example trigger</Trigger>
        <Content>
            <button @onclick="() => Expanded = false">Close</button><br />
            Example content<br />
            Example content<br />
            Example content
        </Content>
    </Popper>
</div>
<Example Identifier="Controls.Popper.Example" />

<h4>Members of <code>Popper</code></h4>
<Members Context="typeof(Popper)" />

@code {
    private bool Expanded { get; set; }
}
